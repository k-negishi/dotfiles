# ============================================================
# ~/.zshenv - 全シェル共通の環境変数（インタラクティブ/非インタラクティブ両方で読まれる）
# PATH など最低限のみ。重い処理は .zshrc に書く
# ============================================================

export EDITOR=vim
